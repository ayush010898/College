def fibonacci(n):
   if n <= 1:
       return n
   else:
       return(fibonacci(n-1) + fibonacci(n-2))

terms = int(input("enter numbers to generate "))
if terms <= 0:
   print("enter a positive integer")
else:
   print("Fibonacci sequence is:")
   for i in range(terms):
       print(fibonacci(i))
